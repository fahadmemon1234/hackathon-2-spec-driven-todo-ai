FROM python:3.11-slim

WORKDIR /app

COPY services/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY services/websocket_service.py .

EXPOSE 8080

CMD ["python", "-m", "uvicorn", "websocket_service:app", "--host", "0.0.0.0", "--port", "8080"]